import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo-navegacion-tabs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
