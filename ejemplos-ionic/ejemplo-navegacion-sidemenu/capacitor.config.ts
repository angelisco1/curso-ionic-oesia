import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ejemplo-navegacion-sidemenu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
